#include "main.h"
#include "helpmerender.h"
#include <iostream>

int main(){
    const char* title = "test";
    //Madd* gameContext = new Madd(800,600,title);
    //gameContext->Start(); 
    xyz a = xyz(1,1,1);
    std::cout << a<<std::endl;
    return 0;
}